export default function AppHomePage() {
  return (
    <main className="appShell">
      <section className="panel">
        <p className="eyebrow">app.zider.ink</p>
        <h1>Wix integration service.</h1>
        <p>
          This project keeps Wix webhook receivers, integration APIs, and app event data away from the public site and
          workspace surfaces.
        </p>
      </section>
    </main>
  );
}
